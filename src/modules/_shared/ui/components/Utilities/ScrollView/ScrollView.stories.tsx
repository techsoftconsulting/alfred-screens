import React from 'react';
import ScrollView from '.';

export default {
    title: 'Global/Utilities/ScrollView',
    component: ScrollView,
    argTypes: {}
};

export const Basic = (args) => <ScrollView {...args} />;

Basic.args = {};
