import React from 'react';
import DateFormatter from '.';

export default {
    title: 'Global/Utilities/DateFormatter',
    component: DateFormatter,
    argTypes: {}
};

export const Basic = (args) => <DateFormatter {...args} />;

Basic.args = {};
